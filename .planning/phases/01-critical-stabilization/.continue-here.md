---
phase: 01-critical-stabilization
task: ongoing (bootstrap + model fixes)
total_tasks: multiple
status: in_progress
last_updated: 2026-04-03T08:44:36.740Z
---

<current_state>
Phase 1 work is mid-flight. Codex model fix done fleet-wide. Zifnab+Hugh back online. Agent bootstrapping process proven and documented. 4 of 20 agents bootstrapped with Death Gate SOULs. 14 remaining. Also discovered and fixed: exec security, Anthropic auth, requireMention on #the-nexus.
</current_state>

<completed_work>

- Codex model fix: `--json` flag added to codex-wrapper on all 3 servers, model refs renamed to `openai-codex/`
- Zifnab + Hugh restarted and responding on Discord
- All 20 agents set to `tools.exec.security: "full"`
- Anthropic token deployed to all 20 agents (auth-profiles.json)
- Anthropic provider (opus + sonnet) added to all 20 agent configs
- Zifnab + Hugh requireMention: true on #the-nexus
- Balthazar bootstrapped (minimax, Death Gate SOUL with 5 absorbed roles from Jarre)
- Ciang bootstrapped (tested on codex — bland, needs better model)
- Edmund bootstrapped (sonnet — BEST result, full lore adoption)
- Alfred files restored from March 11 backup, AGENTS.md updated, needs bootstrap conversation
- Haplo profile workspace archived and deleted, roles/ dir deleted from live
- Agent Reset Runbook written: Chelestra-Sea/infra/AGENT-RESET-RUNBOOK.md
- Timestamped baselines created: Chelestra-Sea/infra/agentbaselines-20260402/
- GSD project initialized with 6 phases, 26 requirements
- Codebase mapped (7 docs)
- Bootstrap model testing: sonnet > minimax > codex > spark for character adoption
</completed_work>

<remaining_work>

- 14 agents need full bootstrap process (SOUL writing, file diffs, reset, Discord message clearing, bootstrap conversation)
  - Dev: Iridal, Jonathon, Limbeck, Marit, Paithan, Trian, Vasu
  - Main: Drugar, Ramu, Rega
  - Trade: Devon, Samah, Sinistrad, Hugh
- Circle back: Haplo (TOOLS.md has Zifnab's content), Zifnab (full diff), Hugh (full diff), Alfred (bootstrap conversation)
- Fleet-wide: Update Your Team in all SOULs, enrich TEAM.md, decide fleet default model
- MCP setup: Fix Discord MCP plugin not loading in Claude Code sessions
- Remaining STAB issues: ACPX permissions, sandbox Docker, old root archiving, orphan service dirs
</remaining_work>

<decisions_made>

- Use OpenClaw auto-created default workspace paths (~/.openclaw/workspace-{name}/) as canonical
- Profile workspace dirs are dead duplicates — archive to repo and delete
- Baselines in repo are OLD backups, not source of truth — live workspaces are truth
- Bootstrap requires: clear sessions + memory DB + Discord messages + delete IDENTITY/USER
- SOUL writing rules: no bullet point roles, roles emerge from Death Gate narrative, 3-4 short paragraphs, tailored Nexus paragraph per agent
- Absorbed roles MUST be in SOUL + AGENTS + OPERATIONS + role files
- Sonnet is best model for bootstrap character adoption
- Anthropic available on all agents as fallback (not primary unless switched)
- Memory DB > 30MB: pause and decide before clearing
- All deliverables go in Chelestra-Sea/ (infra realm), not repo root
- Public repo: no secrets in planning docs
</decisions_made>

<blockers>
- Discord MCP plugin not loading in Claude Code sessions — can't send/read Discord messages from CLI, must use bot API via SSH or manual
- BOOTSTRAP.md keeps disappearing on gateway restart — must verify after every restart
- Gateway auto-creates blank IDENTITY.md + USER.md on start — must delete after restart before bootstrap
</blockers>

<context>
Long session (12+ hours). Owner is exhausted. Process is now well-documented in the runbook. The bootstrap prompt that works best is the long one that introduces Lord Xar and the Nexus mission. Sonnet gave the best results by far — Edmund's bootstrap was exceptional. Next session should be more efficient with the runbook in place. Owner wants to set up MCP servers (Discord plugin especially) to speed up the Discord message management.

For each remaining agent: research Death Gate character → write SOUL → diff all files against Haplo → fix stale refs → push → clear everything → delete Discord messages → bootstrap → verify.
</context>

<next_action>
Start with: Fix Discord MCP plugin loading issue. Then continue agent bootstrapping — next agent is Iridal. Use sonnet for all remaining bootstraps. Follow AGENT-RESET-RUNBOOK.md step by step.
</next_action>
