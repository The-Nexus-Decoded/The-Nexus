---
context: default
phase: issue-450-first-breach-houdini-visual-correction
task: 2
total_tasks: 4
status: awaiting_owner_review
last_updated: 2026-08-20T01:47:39-05:00
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

- [x] CONSTRAINT ACKNOWLEDGED: No QA deployment — The owner rejected the current Houdini composition visually. Do not push a QA release, merge to `qa`, or present this build as ready until corrected local renders receive explicit owner approval.
- [x] CONSTRAINT ACKNOWLEDGED: Semantic sockets only — The current completion algorithm uses `roomId`; passage tiles inherit adjacent room IDs, which placed the ruined altar and a hanging brazier in Passage Two. Non-corridor props must use exact `zoneId`, and corridors may receive only explicitly corridor-safe wall fixtures.
- [x] CONSTRAINT ACKNOWLEDGED: No false substitutions — `training-effigy -> guardian-statue` and `memory-loom -> ruined-altar` are semantically wrong. Keep gameplay objects visually correct and place the guardian/altar only in suitable alcove or boss-room sockets.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|----------------------|
| Asset-presence equals acceptance | The `.hipnc` contained imported models, but several were off-camera, in implausible positions, or visually indistinguishable from placeholders. | blocking | Produce a room-by-room asset ledger and normal-camera screenshot proving every required family is visible, correctly textured, and plausibly placed. |
| Random boundary scatter | Open boundary tiles were treated as interchangeable sockets, allowing room props to enter corridors and focal objects to occupy circulation space. | blocking | Define semantic socket classes: wall, alcove, corner, furniture cluster, floor-edge, ceiling, and corridor-wall; validate clearance before placement. |
| Renderer mismatch hand-wave | The owner saw fire and lighting in Houdini previously; explaining everything as a Three.js-only effect was incomplete. | advisory | Render the actual `.hipnc` through its OpenGL review ROPs and compare it directly with the local Three.js runtime before explaining any difference. |
| Static server release-target mismatch | Serving `dist/client` locally caused an intentional `/play` redirect loop because that artifact expects the ChatGPT Sites worker. | advisory | Use `yarn dev` for interactive proof or serve `dist-pages`; ensure no stale preview process already owns the selected port. |

<current_state>

Repository: `The-Nexus-Decoded/The-Nexus`

- Worktree: `H:\CodexData\.codex\worktrees\0794\The-Nexus-fleet-normalization`
- Game: `Arianus-Sky/projects/games/SoulDrifterWeb`
- Branch: `codex/450-houdini-apprentice-first-breach`
- HEAD at resumed baseline: `e0ec68c4 wip: issue-450 paused at visual audit 1/4`
- Remote branch is still at `ce8ebc3e`; `647314f8` has not been pushed.
- Issue: `#450` (`owner-qa:pending`)
- PR: none
- Worktree: clean before this handoff was added
- Paid 3D operations: none in this resumed task; do not spend credits or regenerate approved assets.

The owner opened `source-assets/houdini/first-breach-apprentice.hipnc` and rejected its current presentation. Specific reports: statues and braziers in implausible circulation areas, gates not visibly matching the imported door/portcullis assets, chains/weapon racks/chests/shelves not clearly present, weaker color/lighting/fire appearance than an earlier view, not enough paintings/wall atmosphere, and uncertainty that randomized rooms remain intact.

The local `?environment=houdini` runtime mode and QA-only budget reporting were implemented and verified technically, but are not visually approved and must not be deployed.

The evidence-only baseline phase is now complete. No scene placement, Houdini source, asset, credit, QA, or deployment mutation occurred during the audit.
</current_state>

<completed_work>

Completed Tasks:

- Existing branch built the full Houdini Apprentice scene, imported 38 GLB environment asset families with extracted PBR textures, and added lights/review cameras.
- Commit `647314f8`: added `?environment=houdini`, locked review seed `4182`, complete kit composition, all review fires lit, modest ambient lift, debug marker, and QA-only report mode for the 150 MB cap while production retains enforcement.
- Verification before pause: 151/151 Vitest tests passed; `yarn typecheck`, QA-mode `yarn build`, `yarn verify:release`, and `git diff --check` passed. QA artifacts were about 149.6 MB.
- Live layout diagnosis: the `roomId` filter allowed `ruined-altar` and `hanging-brazier` placements into `passage-two`. The algorithm also made invalid substitutions for the training effigy and memory loom.
- QA deployment was explicitly paused after the owner rejected the visual state.
- Created `docs/FIRST_BREACH_VISUAL_ACCEPTANCE_SPEC.md`, covering room intent, 38-asset parity, exact semantic sockets, randomization, lighting, effects, gameplay, and release evidence.
- Rendered all five unchanged Houdini ROP baselines to `H:\CodexData\.codex\tmp\souldrifter-450-baseline\houdini\`.
- Captured five unchanged local Three.js `?environment=houdini` baselines to `H:\CodexData\.codex\tmp\souldrifter-450-baseline\threejs\`.
- Three.js proof used seed `4182`, reported no console errors or failed requests, and confirmed the critical runtime failure: skirmish and boss views are effectively black even though assets load.
</completed_work>

<remaining_work>

- Task 3: Correct exporter/runtime composition parity, remove invalid substitutions, restrict room props to exact zones, create corridor-safe fixture sockets, validate both imported gates, and add deliberate wall art/dressing.
- Task 3: Rebuild the `.hipnc`, verify PBR texture maps one-to-one, and ensure fire/lighting/color presentation is visible in the saved Houdini review configuration.
- Task 4: Run tests, typecheck, build, release verification, browser checks, asset request checks, and screenshot comparison. Ask owner to approve visuals.
- Task 4: Only after approval, push branch, open draft PR targeting `qa`, merge/release through the existing QA workflow, and provide the review URL.
</remaining_work>

<decisions_made>

- Local review first; no QA deployment is necessary to see the intended final renderer.
- Houdini and Three.js both require review because Houdini is the authoring source while Three.js supplies final runtime animation, fog, water, fire behavior, and interactions.
- Randomization chooses approved prop sets among authored semantic sockets. It must never scatter focal assets into walkways.
- Full visual parity means each imported GLB keeps its authored geometry and PBR maps; Houdini or runtime fallback primitives are not acceptable when a real approved asset exists.
- The production 150 MB build limit remains enforced. QA and PRs targeting `qa` report size without failing the build.
</decisions_made>

<blockers>

- The implementation phase is paused for owner review of the visual acceptance contract and baseline evidence.
- Visual approval is blocked until corrected local side-by-side renders are ready.
- Current placement algorithm and semantic substitutions are known-bad and must be corrected before rebuilding or deploying.
</blockers>

## Required Reading (in order)

1. `Arianus-Sky/projects/games/SoulDrifterWeb/docs/FIRST_BREACH_VISUAL_ACCEPTANCE_SPEC.md` — blocking visual contract and baseline verdict.
2. `Arianus-Sky/projects/games/SoulDrifterWeb/docs/HOUDINI_FIRST_BREACH_PIPELINE.md` — source/build/export workflow.
3. `Arianus-Sky/projects/games/SoulDrifterWeb/scripts/houdini/export-first-breach-layout.mjs` — current flawed completion placement logic.
4. `Arianus-Sky/projects/games/SoulDrifterWeb/src/game/environment/HoudiniFirstBreachComposition.ts` — runtime mirror of that placement logic.
5. `Arianus-Sky/projects/games/SoulDrifterWeb/scripts/houdini/build-first-breach-apprentice.py` — materials, imported GLBs, fire geometry, lights, cameras, and OpenGL review ROPs.
6. `Arianus-Sky/projects/games/SoulDrifterWeb/docs/3d-ai-studio/first-breach-model-register.json` — approved asset inventory and provenance.
7. `Arianus-Sky/projects/games/SoulDrifterWeb/docs/3d-ai-studio/README.md` — one-to-one asset export and texture contract.

## Critical Anti-Patterns (do NOT repeat these)

- Do not claim the scene is correct because all 38 asset IDs occur in the file. Prove visibility, material fidelity, placement plausibility, and camera readability.
- Do not treat `roomId` as exact room membership; inspect `zoneId`.
- Do not replace a gameplay object with a visually unrelated kit model merely to increase asset coverage.
- Do not deploy first and ask the owner to discover composition errors in QA.
- Do not regenerate or purchase assets; approved source GLBs and texture cache already exist.

## Infrastructure State

- Houdini Apprentice 22.0.368 is installed at `H:\Program Files\Side Effects Software\Houdini 22.0.368`.
- Current `.hipnc`: `Arianus-Sky/projects/games/SoulDrifterWeb/source-assets/houdini/first-breach-apprentice.hipnc`.
- Current external layout audit JSON: `H:\CodexData\.codex\tmp\first-breach-layout-4182.json`.
- Temporary Playwright script: `H:\CodexData\.codex\tmp\souldrifter-houdini-qa-check.py`.
- Do not assume port 4174/4175 is free; stale preview processes caused a false redirect-loop diagnosis. Resolve exact PIDs before stopping only processes created by this workflow.
- No QA release or deployment was performed.

## Pre-Execution Critique Required

- Design artifact to create: `Arianus-Sky/projects/games/SoulDrifterWeb/docs/FIRST_BREACH_VISUAL_ACCEPTANCE_SPEC.md`.
- Critique focus: Does every focal prop have a plausible socket? Are both routes/gates visually distinct and unobstructed? Are the soul well, wall art, shelving, weapons, chains, chests, rubble, braziers, and room-specific clues visible at gameplay camera distance? Does randomization preserve authored composition and traversal? Do Houdini and Three.js evidence use comparable cameras and lighting intent?
- Gate: Do not edit the placement implementation until the baseline audit and visual acceptance spec are complete.

<context>

The user wants a beautiful, realistic Diablo-style First Breach with readable ambient fill, motivated firelight, strong shadows, aged masonry/floors, organic history, and approved 3D Studio models shown exactly with their authored textures. They are frustrated by repeated iterations that technically include assets but place or present them poorly. Treat visual evidence and semantic placement as first-class acceptance, not polish after implementation.
</context>

<next_action>

Owner reviews `docs/FIRST_BREACH_VISUAL_ACCEPTANCE_SPEC.md` and the ten baseline images under `H:\CodexData\.codex\tmp\souldrifter-450-baseline\`. If the contract is accepted, begin Task 3 with exact-zone/socket corrections; do not deploy.
</next_action>
