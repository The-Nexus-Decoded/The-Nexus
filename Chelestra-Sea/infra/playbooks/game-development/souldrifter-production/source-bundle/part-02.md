n/model/animation runbook.
4. The ticket contract generated by Requirement Compiler.
5. Only then the source files required for its assigned work.

The orchestrator should not paste all project knowledge into every worker. Give each worker:
- binding project rules,
- its atomic task slice,
- relevant dependencies,
- expected artifacts,
- acceptance checks,
- current ledger state.

## Agent Team behavior

M3 can parallelize tasks, but parallel work is allowed only when file ownership and dependencies do not overlap.

Examples:
- PBR/material work and quest-dialog data may run in parallel if they touch different modules.
- Door geometry and corridor/nav work should normally remain one dependency chain.
- Character source inventory audit can run before rigging.
- Independent visual verification must run after the producer commits.

## Non-negotiable anti-forgetting rule

At the end of every worker turn, the worker must update the shared completion ledger and handoff state. It may not summarize completion only in chat.

No ledger update = work is treated as unfinished.
```

---

## `source/ONBOARDING.md`

SHA-256: `be4049569442051e68f852df3cce8871db0aaf36a72c396dfd34d058f0e99530`

```markdown
# SoulDrifter Multi-LLM Onboarding

**This gate happens before GLOBAL-AUDIT or any ticket implementation.**

## Authoritative local workspace

Main repository checkout:

`H:\Projects\AI_Tools_And_Information\The-Nexus`

SoulDrifter game root:

`H:\Projects\AI_Tools_And_Information\The-Nexus\Arianus-Sky\projects\games\SoulDrifterWeb`

Ticket worktrees belong under:

`H:\CodexData\.codex\worktrees\<issue>\...`

Repository:

`The-Nexus-Decoded/The-Nexus`

## Hard rules

- Do not create a fresh clone.
- Do not silently use another copy of The-Nexus.
- Do not work from Downloads/Desktop/temp copies.
- Do not put multiple ticket workers in the same worktree.
- Main checkout is for orientation/integration, not concurrent ticket implementation.
- One ticket = one branch + one dedicated worktree.
- Never write credentials/tokens into the repo.

---

# Gate A — Automatic workspace/worktree discovery

**Do not require the owner to manually select a ticket workspace.**

First read `AUTO_DISCOVER_WORKSPACE.md`.

Every model/session must:

1. inspect its current directory for an existing The-Nexus checkout/worktree;
2. locate the canonical local The-Nexus checkout if needed;
3. run `git worktree list --porcelain`;
4. discover existing ticket worktrees/branches;
5. map ticket -> local worktree -> branch -> live PR/issue;
6. reuse in-progress worktrees instead of creating duplicates;
7. prove the SoulDrifter game root and harness are accessible.

A new worktree is a last resort only when no existing ticket worktree exists.

Minimum local evidence:

```text
pwd / current workspace root
git rev-parse --show-toplevel
git branch --show-current
git status --short
git remote -v
```

The main checkout may be:
`H:\Projects\AI_Tools_And_Information\The-Nexus`

Ticket work should normally resolve to an **existing discovered worktree** under the configured worktree system.

If no valid local checkout/worktree can be discovered:
**STOP — WORKSPACE_DISCOVERY_BLOCKED**.

---

# Gate B — GitHub connection verification

The session must prove it can read the live repository:

`The-Nexus-Decoded/The-Nexus`

It must be able to retrieve:
- repository metadata;
- open SoulDrifter issues;
- issue comments;
- PRs/branches needed for its ticket.

If the environment uses a GitHub CLI/account integration, verify that integration before edits.
If it uses a built-in connector, perform a live read.

Do not assume that local Git access means the GitHub issue/PR integration is also authenticated.

If read access fails:
`BLOCKED_GITHUB_CONNECTION`

Writes to GitHub remain subject to the normal owner/agent authorization rules.

---

# Gate C — Sync / freshness verification

Before ticket work:

1. identify configured remote;
2. fetch remote refs if permitted;
3. compare current branch/worktree against its expected upstream/base;
4. inspect uncommitted changes;
5. do not overwrite unrelated local work;
6. read the live GitHub issue/comments even if local docs exist.

A clean `git status` alone does not prove freshness.

If the branch is behind/conflicted or contains unexplained work:
stop and classify the condition before editing.

---

# Gate D — MiniMax M3 built-in Agent Team discovery

The sidebar may contain MiniMax-provided default agents such as:
- Explore
- Worker
- Coder
- Verifier
- General

**Do not assume their hidden prompts, tools, permissions, model, or context behavior from their names.**
They are product defaults unless the owner explicitly created/customized them.

At first M3 onboarding:

1. inspect available details/settings for each built-in agent if the UI exposes them;
2. record what is actually observable:
   - name;
   - model if visible;
   - tools/permissions if visible;
   - workspace access;
   - GitHub access;
   - whether it inherits project context;
   - whether it can edit files;
3. do not modify/delete defaults merely to match this harness;
4. test one harmless read-only dispatch before trusting context propagation;
5. require every dispatched agent to read `START_HERE.md` and return a Context Receipt regardless of preset.

## Custom M3 agents

Create a custom persistent agent only if the built-in team cannot reliably perform a required role.

Likely custom roles, only if needed:
- Production Orchestrator
- Requirements/Canon Auditor
- Independent SoulDrifter Verifier
- Character/Tripo Specialist
- World/Houdini Specialist
- Combat/Animation/VFX Specialist

Do not create all six preemptively.

---

# Gate E — Claude Code onboarding

Claude Code must use the exact same repository and ticket-worktree contract.

Before work it must prove:
- correct top-level repo/worktree;
- live GitHub issue/PR access if available in its environment;
- `START_HERE.md` loaded;
- context version matches;
- ticket state loaded;
- no conflicting work claim.

Claude-specific memory/instructions are adapters only.
They do not override the shared harness.

---

# Gate F — ChatGPT / Codex onboarding

ChatGPT/Codex follows the same checks.

A connected GitHub account does not eliminate the local-workspace check.
A local Codex workspace does not eliminate the live-GitHub check.

Both must be correct before implementation.

---

# ONBOARDING RECEIPT

Before GLOBAL-AUDIT, produce:

```text
SOULDRIFTER ONBOARDING RECEIPT
contextVersion: <version>
platform: <M3|Claude Code|ChatGPT/Codex>
workspaceRoot: <path>
repositoryTopLevel: <path>
repositoryRemote: <remote>
repositoryIdentity: The-Nexus-Decoded/The-Nexus
gameRootExists: yes/no
githubLiveRead: PASS/FAIL
branch: <branch>
worktree: <path>
gitStatusReviewed: yes
remoteFreshnessReviewed: yes
harnessEntryPointRead: yes
builtInAgentPresetsInspected: <M3 only: yes/no/not exposed>
agentContextPropagationTested: <M3 only: yes/no>
blockingIssues: []
result: PASS | BLOCKED
```

No PASS = do not begin the project audit.
```

---

## `source/OPENING_PROMPTS.md`

SHA-256: `7c096090b0621a343f05368f8457fb884f4b74fcd206ad7cebd13cf3b1345cfe`

```markdown
# Opening Prompts for Every New Chat / Agent Session

## 1. FIRST SESSION — GLOBAL ORCHESTRATOR AUDIT

```text
You are the SoulDrifter Production Orchestrator.

Do not begin implementation yet.

Read the binding game AGENTS.md and the complete SoulDrifter Multi-LLM Master Harness beginning with START_HERE.md. Follow its startup order and return a Context Receipt with ticket=GLOBAL-AUDIT.

Then query GitHub for every currently open SoulDrifter issue and related open PR. Read each issue's current comments before classifying it.

Audit all open work against current code/canon and the new harness. For each issue classify it as CONTINUE_VALID, REVALIDATE, REWORK_REQUIRED, SUPERSEDED_OR_LEGACY, BLOCKED, OWNER_DECISION_REQUIRED, or CLOSE_CANDIDATE_AFTER_VERIFICATION.

For every issue identify:
- latest owner intent;
- current implementation/PR/worktree state;
- requirements already genuinely verified;
- stale claims;
- conflicts with current Tripo/modular-character/combat/VFX/current-phase rules;
- dependencies on other tickets;
- whether it can run in parallel safely;
- recommended worker role;
- recommended verifier;
- next atomic action.

Create/update machine-readable ticket state, but do not merge, deploy, close tickets, or perform broad implementation in this audit session.

Finish with a prioritized parallel execution board: NOW / NEXT / BLOCKED / OWNER DECISION.
```

## 2. NORMAL TICKET WORKER CHAT

```text
Work SoulDrifter GitHub issue #<ISSUE> as role <ROLE>.

Before editing, read START_HERE.md from the SoulDrifter Multi-LLM Master Harness and follow every mandatory startup step. Fetch the issue, all current comments, linked PRs, current branch/worktree, and .agent-state/<ISSUE> state. Return a valid Context Receipt first.

Treat repository state as authoritative; do not rely on another chat's memory.

Compile/reconcile the ticket requirements against current code/canon and the master harness. Work only in the dedicated branch/worktree for #<ISSUE>. Do not fix unrelated tickets unless they are an explicit dependency and the orchestrator re-routes the work.

Implement in dependency order. Run required tests and capture evidence. Update the completion ledger and handoff after each atomic milestone.

You may mark your work only IMPLEMENTED_UNVERIFIED. Do not call the ticket done, merge, deploy, or self-verify. End by identifying exactly what the independent verifier must test.
```

## 3. INDEPENDENT VERIFIER CHAT

```text
Independently verify SoulDrifter issue #<ISSUE>.

Read START_HERE.md and complete the mandatory startup flow. Return a Context Receipt with role=independent-verifier.

Fetch the original issue, all latest owner comments, linked PR/diff, current completion ledger, evidence manifest, and producer commits. Do not assume the producer's summary is correct.

Re-derive expected requirements independently. Run fresh tests/runtime checks and inspect fresh evidence. Actively look for incomplete relationships, clipping, missing connectors, wrong orientations, bad deformation, missing reactions, stale screenshots, placeholders, and untested matrix rows.

For each requirement return PASS, FAIL, or NEEDS_EVIDENCE. Only you may move producer rows from IMPLEMENTED_UNVERIFIED to VERIFIED.
```

## 4. RESUME IN A NEW CHAT

```text
Resume SoulDrifter issue #<ISSUE> from repository state, not conversation memory.

Read START_HERE.md, fetch issue/comments/PR, and load .agent-state/<ISSUE>/ticket-contract.json, completion-ledger.json, evidence-manifest.json, and handoff.json. Inspect actual worktree and recent commits.

Return a Context Receipt and state the next atomic requirement ID before work.

Do not redo VERIFIED requirements. Continue only unresolved, blocked, or IMPLEMENTED_UNVERIFIED work assigned to your role.
```

## 5. CHARACTER / TRIPO ADDITION

```text
Use the current Tripo-first pipeline and modular-character contract. Do not resurrect the legacy Mixamo-first plan unless Tripo/custom work fails and the owner explicitly accepts fallback.

Playable characters are modular: neutral base body, separate head/face/hair/accessories/gear/weapons. NPCs may use full-outfit generation plus segmentation. Monsters use regenerate/compare QA.

Do not spend provider credits on an unapproved batch. Pilot -> verify -> fan out.
```

## 6. COMBAT / SKILL ADDITION

```text
Use the source-grounded skill-design gate before inventing/changing skills.

Current phase is Level 1–9/First Breach/Heartvale: no Sartan/Patryn/Labyrinth/rune/possibility combat leakage.

Every starter class targets three active abilities; at least two must form a readable setup/opening/payoff relationship. Define per-skill cooldowns, class resource, target reaction, animation/contact markers, VFX/SFX, UI feedback, and both combat modes.

Real-time is default. Turn-based uses the same unde