# SoulDrifter Harness Source Bundle

This directory is the GitHub-friendly archival form of the complete SoulDrifter Multi-LLM Master Harness v3 plus the preserved earlier agent starter-pack source.

The source was concatenated in deterministic path order and split into numbered chunks only to fit repository/API transport limits.

## Reconstruct

Concatenate in numeric order:

`part-01.md` through `part-19.md`

The resulting text contains every archived source file, each preceded by its original relative path, SHA-256 hash, and fenced contents.

## Canonical-use warning

The parent `START_HERE.md` has a small archival correction: its context version is explicitly `2026-08-23-master-v3`, matching `config/context-version.json`. The chunked source bundle preserves the original packaged v3 snapshot for provenance, including any internal historical mismatch.

For active work, use the parent `START_HERE.md`, `CURRENT_DIRECTION.md`, and current live SoulDrifter repository/code/tickets before consulting archived legacy source.
