---
phase: 01-infrastructure-and-notifications-foundation
task: 3
total_tasks: 4
status: in-progress
last_updated: 2026-02-22T10:30:00.000Z
---

<current_state>
Plans 01-01 and 01-02 are COMPLETE on ola-claw-dev. The dev server is fully provisioned, hardened, and running OpenClaw gateway on port 18789. Plans 01-03 (Discord/notifications) and 01-04 (backup) remain.

Only the dev server is set up. The other 2 servers (ola-claw-main, ola-claw-trade) are pending — motherboards have bent pins. If they can't be fixed, all 3 personalities will run on dev server on different ports.
</current_state>

<completed_work>

- Plan 01-01: Server provisioning + SSH hardening — COMPLETE on dev
  - Tailscale installed and connected (IP: 100.82.243.72)
  - UFW enabled (Tailscale-only traffic)
  - Fail2ban running
  - SSH hardened (key-only, Tailscale CGNAT range)
  - openclaw user with NOPASSWD sudo
  - Unattended security upgrades enabled
- Plan 01-02: OpenClaw installation — COMPLETE on dev
  - Node.js 22 installed via NodeSource
  - OpenClaw installed via npm (v2026.2.21-2)
  - Gateway running on port 18789 (gateway.mode=local)
  - Dev Factory SOUL.md deployed
  - OpenClaw workspace at /data/openclaw/ (symlinked from ~/.openclaw)
  - Systemd service installed and enabled
- GitHub repo created: github.com/olalawal/openclaw-homelab (private)
- Ansible installed on dev server (runs locally + will target other servers)
- gh CLI authenticated on both Windows and dev server
- All Ansible fixes committed — other servers will work first try
</completed_work>

<remaining_work>

- Plan 01-03: Discord bot channel config and iMessage/ntfy.sh critical alert setup
- Plan 01-04: Data drive already set up (/data exists). Remaining: weekly rsync backup to Windows PC over Tailscale
- Set up ola-claw-main and ola-claw-trade when motherboards are fixed
</remaining_work>

<decisions_made>

- Ansible runs FROM dev server, not Windows (no WSL needed)
- Ubuntu installed on NVMe (not SSD as planned) — LVM extended to full 1.8TB
- gateway.mode=local required before gateway starts
- openclaw gateway install/start (not onboard --install-daemon)
- Dev server has 64GB RAM (not 16GB)
- Crypto SOUL.md includes pattern analysis, wallet tracking, ore.supply/godl.supply farming
- Dev Factory SOUL.md supports autonomous builds, PRs, integration deployment
- If other motherboards fail, run all 3 personalities on dev (ports 18789/18790/18791)
</decisions_made>

<blockers>
- 2 motherboards have bent pins — may not get other servers running
- Tailscale auth key exposed in chat — needs revocation and regeneration
</blockers>

<context>
The dev server is the first of 3 planned servers. It's fully operational with OpenClaw running. The owner is new to Linux but successfully SSHed in and ran playbooks with guidance. All issues encountered during setup have been fixed in the Ansible roles so future server provisioning should be smoother.
</context>

<next_action>
Next session — EXACT STEPS in order:
1. User has installed second GTX 1070 into dev server. Both machines back online.
2. SSH into dev server: ssh openclaw@ola-claw-dev
3. Verify both GPUs detected: nvidia-smi (should show 2x GTX 1070)
4. If NVIDIA drivers not installed: sudo apt install nvidia-driver-535
5. Install Ollama: curl -fsSL https://ollama.com/install.sh | sh
6. Pull model: ollama pull qwen2.5-coder:14b (auto-splits across both GPUs)
7. Test locally: ollama run qwen2.5-coder:14b "hello"
8. Configure OpenClaw to use Ollama as provider: openclaw configure → Model → Ollama
9. Verify Discord bot responds in #coding channel
10. Then continue with Plan 01-03 (ntfy.sh alerts) and 01-04 (backup)

Auth decision: Anthropic blocked OAuth for third-party tools (Jan 2026). Using Ollama local LLMs (free) now. API keys for Anthropic/OpenAI deferred to next month when budget allows.
</next_action>
