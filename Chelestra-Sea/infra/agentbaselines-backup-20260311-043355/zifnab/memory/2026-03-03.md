# Zifnab's Memory | 2026-03-03

## Key Decisions Made
- **Model selection finalized**: Downloading instruction-tuned Qwen3.5 models (2B, 4B, 9B) only; base models deferred for potential future training.
- **vLLM installation approach**: Using pip directly on ola-claw-main server; initial attempt failed due to network timeout during large wheel download (~899.7 MB).
- **Fleet monitor removal**: Successfully deleted rate-guard-monitor script; Rate Guard proxy remains active on port 8787.

## Current Project Status
### vLLM Installation
- **Status**: FAILED (session `mellow-cedar`)
- **Details**: Download stalled at ~716 MB (79%) with speed degradation from 4+ MB/s to <1 MB/s. Likely network timeout or connection instability.
- **Next**: Retry with extended timeout; will monitor network stability.

### Model Downloads (in progress)
- `Qwen3.5-2B` (~4GB) — session `vivid-lobster` (running)
- `Qwen3.5-4B` (~8GB) — session `plaid-reef` (running)
- `Qwen3.5-9B` (~18GB) — session `salty-summit` (running)
- Estimated completion: 30–60 minutes

### Wallet Analyzer
- **Status**: COMPLETE (2026-03-03 16:15 UTC)
- Results:
  - Owner wallet: 73,660 transactions, 738 pages, 516 MB
  - Wallet2: 594 transactions, 7 pages, 2.7 MB
  - Bot wallet: 933 transactions, 11 pages, 4.4 MB

### Trading Pipeline (Hugh)
- No new updates; infrastructure remains ready for live-fire trades.
- Meteora pipeline operational with rebalancing logic implemented by Haplo.

## Lessons Learned
- **Large package downloads**: PyPI wheels >800 MB are prone to network timeouts; implement retry logic with resume support or use mirrors.
- **Session management**: Multiple background sessions running simultaneously can make status tracking difficult; consider naming conventions with clear purpose tags.
- **Network stability**: Tailscale connectivity should be verified before initiating massive downloads; consider running `fleet health` check first.

## Active Blockers
1. **vLLM installation failed** — needs retry with better error handling
2. **Git clone verification** — need to confirm LFS pointers are properly resolved and `pytorch_model.bin` files are fully downloaded after each model clone completes
3. **Disk space** — should verify `/data` has sufficient free space (vLLM build artifacts + models require ~50-60 GB total)

## Other Notes
- Fleet monitor script successfully removed; no cron/systemd references found.
- All model downloads are Git LFS clones; will verify integrity after completion.
- User requested avoidance of acknowledgment messages when no new information; will only post substantive updates.