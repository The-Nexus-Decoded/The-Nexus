import { XRMessage, ManipulationIntent, PresentationMode, ReconciliationMode } from './types';
type ConnectionState = 'disconnected' | 'connecting' | 'connected' | 'reconnecting';
type MessageHandler = (message: XRMessage) => void;
export declare class XRWebSocketClient {
    private ws;
    private url;
    private state;
    private handlers;
    private pendingIntents;
    private presentationMode;
    private reconnectAttempts;
    private maxReconnectAttempts;
    private reconnectDelay;
    private messageQueue;
    private intentQueue;
    private reconciliationMode;
    constructor(url: string);
    setReconciliationMode(mode: ReconciliationMode): void;
    getReconciliationMode(): ReconciliationMode;
    connect(): Promise<void>;
    disconnect(): void;
    private setState;
    private attemptReconnect;
    onMessage(handler: MessageHandler): () => void;
    private handleMessage;
    private handleResponse;
    private send;
    private flushMessageQueue;
    sendState(state: PresentationMode, ttl_ms?: number): void;
    setPresentationMode(mode: PresentationMode): void;
    getPresentationMode(): PresentationMode;
    sendIntent(intent: ManipulationIntent): void;
    private handleIntentTimeout;
    sendGesture(gestureType: string, confidence: number, hapticsApplied: boolean): void;
    getPendingIntents(): ManipulationIntent[];
    clearPendingIntents(): void;
    getConnectionState(): ConnectionState;
    isConnected(): boolean;
}
export declare function createXRWebSocketClient(url: string): XRWebSocketClient;
export declare function createManipulationIntent(action: ManipulationIntent['action'], axis: ManipulationIntent['axis'], method: ManipulationIntent['method'], confidence?: number): ManipulationIntent;
export {};
